#given a collection of strings, find and create the single superstring
#fasta format. strings are all the same length
#string overlap will be more than half their length



#if len(overlap) is > (half the length of the substrings) and :
    #call function